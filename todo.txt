openfds que sean un array con el mapeo

processes con el inicializador para fds y prio

refactor de los bpp

piano y spotify(osea con algunas canciones ya puestas)

una implementacion de stack de ints con array con capacity

refactor timer tick handler para que sea directamente el del scheduler

que time no cuente ticks ya que lo va a hacer el scheduler, que provea funcion para los ms elapsed y refactorizar el sleep con force reschedule

que el videodriver se llame video y sus funciones empiecen video_

que las syscalls que devuelven info en un struct lo hagan con un parametro de salida (no es lo mas prolijo pero ya que va a ser desprolijo en un lugar que sea desprolijo en todos no?)

registros en struct y cambiar tecla de snapshot

pantalla de inicio

constante de validacion para numero de syscalls?

ordenar archivos a carpetas como me gusten en kernel

--------

correr un formatter mejor

poner gdb

-------

rework mm -> tener uno solo, inicializarlo piola

ver cleanup resources (leaks en bg)

ver que es necesario y que no del scheduler

refactor semaforos para usar queue_t

---- video

rework driver de video y que solo el de fg pueda dibujar

todas las funciones de dibujo a graphics.c en userland (incluyendo las de strings)

syscalls:
    typedef region_t

    habilitar y deshabilitar textmode

    info de video

    un get de un framebuffer

    present

    present_region

    present_nregions
----

rework driver de teclado -> flechitas y lo del keystatus con scancode

historial de comandos en la shell

jueguito con delta t

manejo de excepciones que rebootee la compu? calculadora para poder probar el div0



