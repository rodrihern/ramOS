

---- video

pantalla de inicio

rework driver de video y que solo el de fg pueda dibujar

rework de tty para tener un buffer[max_rows * max_cols]

ctrl+ y ctrl- para cambiar tamaño de la tty desde el keyboard

hacer un frambuffer adt para las funciones de dibujo (incluyendo algunas de strings con fonts)


syscalls:
    rehacer la de sys_ticks de gede y para el jueguito

    typedef region_t

    habilitar y deshabilitar tty -> solo el de fg puede
    con hacer un present se deshabilita solo la tty

    cambiar background -> sys_tty_bgcolor

    info de video

    present

    present_region

    present_nregions

----

tengo que ver que pasa si quieren cosas de teclado y los crean en background

jueguito con delta t

piano

doom

--------

correr un formatter mejor

poner gdb

-------

---- procesos

hacer un dup(?)

refactorizar la inicializacion del proceso -> 
    hereda los BUILTIN_FD
    luego recibe remapeos tipo dup que son los del padre
    recibe en la inicializacion si pasa a ser hijo de init (en ese caso no heredaria nada)

ver si quiero seguir teniendo:
    // syscalls para foreground process
    sys_set_foreground_process(int pid);
    sys_adopt_init_as_parent(int pid);
    sys_get_foreground_process(void);

limpiar el scheduler


ver cleanup resources (leaks en bg)

----




